name: powerdns-admin-test

services:
  python-tests:
    image: powerdns-admin-python-tests
    build:
      context: ..
      dockerfile: common/Dockerfile.app
      args:
        DOCKER_SCENARIO: test
        INSTALL_TEST_DEPENDENCIES: "1"
    networks:
      - test
    env_file:
      - test/env
    environment:
      FLASK_CONF: /app/configs/docker_config.py
      PDNS_API_KEY: ${PDNS_API_KEY:-changeme}
    tmpfs:
      - /data
    depends_on:
      - pdns-server

  pdns-server:
    image: powerdns-admin-python-tests-pdns
    build:
      context: ..
      dockerfile: common/Dockerfile.pdns
    networks:
      - test
    env_file:
      - test/env
    environment:
      PDNS_API_KEY: ${PDNS_API_KEY:-changeme}
      PDNS_RESET_DATABASE: "1"

networks:
  test:
